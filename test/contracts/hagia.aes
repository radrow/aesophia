contract C =
//  entrypoint max(x : int, y : int) = if(x < y) y else x

//  entrypoint chuj(x : int) = if(x < 0) 0 else x

  entrypoint f(x:int) = x + 1
  entrypoint g(y) =
    let q = f(-1)
    1 + q
